# backlog

Deferred work, ideas, and low-priority improvements. Do not start without asking.

## API shape — LOW

- Unified PipelineBuilder for both graphics and compute (currently split into GraphicsPipelineBuilder + createComputePipeline free function)

## features — LOW

- VMA (Vulkan Memory Allocator) integration if allocation patterns become painful
